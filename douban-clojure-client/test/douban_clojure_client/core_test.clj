(ns douban-clojure-client.core-test
  (:require [clojure.test :refer :all]
            [douban-clojure-client.core :refer :all]))

(deftest get-douban-test
  (testing "FIXME, I fail."
    (is (= (get-douban) 200))))

(deftest auth-test
  (testing "FIXME, I fail."
    (is (= (auth) 200))))
